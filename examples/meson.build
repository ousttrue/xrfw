# plog_dep = dependency('plog')
# openxr_loader_dep = dependency('openxr_loader_windows_prebuilt')

executable('xrfw_sample', [
    'main.cpp'
],
    install: true,
    dependencies: [openxr_loader_dep, plog_dep, xrfw_dep],
)
